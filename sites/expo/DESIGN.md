# AegisAI EXPO design source

Stitch project `16781371013844496348`, screen `dc36fb14f2ac46a58c3a44df671a1a6d`, requested model `GEMINI_3_1_PRO`.

Adapted the generated mobile order lookup card, Swiss editorial hierarchy, ink/navy text, cobalt primary action, pale canvas, fine borders, and three-step ribbon into a responsive interactive site. Applied accessible type sizes, unrestricted zoom, local system fonts and bundled icons. All runtime interactions and anonymous storage are implemented locally, not copied as claims from the generated screen.

The fictional scenario illustrates SQL injection. No real customer orders, credentials, production scan results, performance statistics, or live model outputs appear on this Site.

## Comparison requested by the user

Both designs are approved for deployment at the existing domain under `/expo1` and `/expo2`. `/` and `/compare` show interactive mobile previews. `/stitch` preserves the original design adaptation. `/direct` is an independently authored second design, with no Stitch generation: a dark release-check mission, high-contrast lime actions, fictional order receipts, and an exposed-order explanation. Both variants share the same anonymous interest endpoint and advisory-only sample scope.

Stitch entries above document version A's provenance only. They are not design-tool instructions. The user requested removing mandatory Stitch guidance from the repository.

