# Dashboard Backend Module Plan

1. Add failing backend dashboard tests for service aggregation and controller response wrapping.
2. Add failing frontend dashboard API helper coverage for `/dashboard`.
3. Implement shared dashboard workspace contract.
4. Implement `DashboardService`, `DashboardController`, and `DashboardModule`.
5. Wire `DashboardModule` into `AppModule`.
6. Replace frontend dashboard aggregation helper with a single API fetch.
7. Update OpenAPI coverage and re-run validation.
